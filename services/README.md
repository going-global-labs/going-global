# Services

这里预留跨应用或应用内部的服务边界。需要后台能力时，不要继续把业务逻辑堆进单个 HTML 文件。

可能的服务：

- `calendar-api`：日历数据、用户模板和导出任务；
- `content-sync`：定时同步和数据核验；
- `notification-worker`：邮件或提醒任务。

服务必须拥有独立的环境变量、数据库访问权限、日志和部署配置。服务目录为空时不代表已经有后端，当前站点仍是静态 MVP。
